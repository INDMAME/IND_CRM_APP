Exportfile for AOT version 1.0 or later
Formatversion: 1

***Element: CLS

; Navision Axapta Class: INDInternalApiClientServer descargados en Viernes 24/07/2026
; --------------------------------------------------------------------------------
  CLSVERSION 1
  
  CLASS #INDInternalApiClientServer
    PROPERTIES
      Name                #INDInternalApiClientServer
      Extends             #
      RunOn               #Server
    ENDPROPERTIES
    
    METHODS
      Version: 3
      SOURCE #classDeclaration
        #class INDInternalApiClientServer
        #{
        #}
      ENDSOURCE
      SOURCE #deleteBlobFileByUrl
        #static server boolean deleteBlobFileByUrl(
        #    str _baseUrl,
        #    str _username,
        #    str _password,
        #    str _urlFileId,
        #    str _correlationId)
        #{
        #    INDInternalApiClient           clientAPI;
        #    str                            correlationId;
        #    boolean                        deleted;
        #    ;
        #
        #    correlationId = _correlationId;
        #
        #    if (!correlationId)
        #    {
        #        correlationId = strFmt("AX-API-BLOB-DELETE-%1", timeNow());
        #    }
        #
        #    try
        #    {
        #        // The DLL logs in, calls the API endpoint, parses the JSON and returns bool.
        #        clientAPI = new INDInternalApiClient();
        #        deleted = clientAPI.DeleteBlobFileByUrl(
        #            _baseUrl,
        #            _username,
        #            _password,
        #            _urlFileId,
        #            correlationId);
        #    }
        #    catch (Exception::Error)
        #    {
        #        deleted = false;
        #    }
        #
        #    clientAPI = null;
        #
        #    return deleted;
        #}
      ENDSOURCE
      SOURCE #isApiOnline
        #static server boolean isApiOnline(str _baseUrl, str _correlationId)
        #{
        #    INDInternalApiClient           clientAPI;
        #    str                            correlationId;
        #    boolean                        online;
        #    ;
        #
        #    correlationId = _correlationId;
        #
        #    if (!correlationId)
        #    {
        #        correlationId = strFmt("AX-API-HEALTH-%1", timeNow());
        #    }
        #
        #    try
        #    {
        #        // Wrapper generated by the Axapta Business Class Wizard with mask INDIntApi.
        #        clientAPI = new INDInternalApiClient();
        #        online = clientAPI.IsApiOnline(_baseUrl, correlationId);
        #    }
        #    catch (Exception::Error)
        #    {
        #        online = false;
        #    }
        #
        #    clientAPI = null;
        #
        #    return online;
        #}
        #
      ENDSOURCE
      SOURCE #normalizeInternalApiMailImportance
        #// Normaliza la importancia aceptando texto Graph o el valor numerico del enum INDEmailImportance.
        #static server str normalizeInternalApiMailImportance(str _importance)
        #{
        #    str importance;
        #    ;
        #
        #    importance = strLwr(strLRTrim(_importance));
        #
        #    if (importance == 'low' || importance == '0')
        #        return 'low';
        #
        #    if (importance == 'high' || importance == '2')
        #        return 'high';
        #
        #    // Valor por defecto: normal. Tambien cubre 'normal', '1' y valores no reconocidos.
        #    return 'normal';
        #}
        #
      ENDSOURCE
      SOURCE #resolveInternalApiConnection
        #// Resuelve la configuracion de IND_INTERNAL_API usando la misma fuente que los jobs manuales.
        #static server container resolveInternalApiConnection()
        #{
        #    INDDefaultParameters defaultParameters;
        #    str                  baseUrl;
        #    str                  clientId;
        #    str                  clientSecret;
        #    ;
        #
        #    defaultParameters = INDDefaultParameters::find();
        #    baseUrl          = defaultParameters.InternalAPIUrlService;
        #    clientId         = defaultParameters.APIUserId;
        #    clientSecret     = defaultParameters.INDAPIIntServiceUser().Password;
        #
        #    return [baseUrl, clientId, clientSecret];
        #}
        #
      ENDSOURCE
      SOURCE #sendInternalApiMailEx
        #// Sends email through the only supported DLL mail method: SendMailEx.
        #static server boolean sendInternalApiMailEx(
        #    str _companyId,
        #    str _fromEmail,
        #    str _fromDisplayName,
        #    str _toEmails,
        #    str _ccEmails,
        #    str _bccEmails,
        #    str _replyToEmails,
        #    str _subject,
        #    str _htmlBody,
        #    str _textBody,
        #    str _attachmentFilePaths,
        #    boolean _saveToSentItems,
        #    str _importance,
        #    str _sourceProcess,
        #    str _eventType,
        #    str _aggregateType,
        #    str _aggregateId,
        #    str _idempotencyKey,
        #    str _correlationId = '')
        #{
        #    COM                            clientAPI;
        #    str                            baseUrl;
        #    str                            clientId;
        #    str                            clientSecret;
        #    container                      connectionInfo;
        #    str                            importance;
        #    boolean                        sentOk;
        #    ;
        #
        #    connectionInfo = INDInternalApiClientServer::resolveInternalApiConnection();
        #    baseUrl        = conPeek(connectionInfo, 1);
        #    clientId       = conPeek(connectionInfo, 2);
        #    clientSecret   = conPeek(connectionInfo, 3);
        #
        #    if (!baseUrl || !clientId || !clientSecret)
        #    {
        #        warning("No se envio email: falta configuracion de IND_INTERNAL_API.");
        #        return false;
        #    }
        #
        #    if (!_fromEmail || !_toEmails || !_subject || (!_htmlBody && !_textBody))
        #    {
        #        warning("No se envio email: faltan From, To, Subject o Body.");
        #        return false;
        #    }
        #
        #    importance = INDInternalApiClientServer::normalizeInternalApiMailImportance(_importance);
        #
        #    info(strFmt('IND_INTERNAL_API mail: preparando SendMailEx company=%1 sourceProcess=%2 eventType=%3 aggregateType=%4 aggregateId=%5 correlationId=%6 attachmentFilePathsLength=%7 saveToSentItems=%8 importance=%9.',
        #                _companyId,
        #                _sourceProcess,
        #                _eventType,
        #                _aggregateType,
        #                _aggregateId,
        #                _correlationId,
        #                strLen(_attachmentFilePaths),
        #                _saveToSentItems,
        #                importance));
        #
        #    try
        #    {
        #        // attachmentFilePaths contains staged absolute paths separated by semicolon.
        #        // The DLL reads those files from AOS, infers type and converts them to base64.
        #        clientAPI = new COM("IND.InternalApiClient");
        #        info(strFmt('IND_INTERNAL_API mail: invocando COM SendMailEx parameterCount=23 aggregateId=%1 correlationId=%2.', _aggregateId, _correlationId));
        #        sentOk = clientAPI.SendMailEx(
        #            baseUrl,
        #            clientId,
        #            clientSecret,
        #            _fromEmail,
        #            _fromDisplayName,
        #            _toEmails,
        #            _ccEmails,
        #            _bccEmails,
        #            _replyToEmails,
        #            _subject,
        #            _htmlBody,
        #            _textBody,
        #            _attachmentFilePaths,
        #            _saveToSentItems,
        #            importance,
        #            _companyId,
        #            'AXAPTA',
        #            _sourceProcess,
        #            _eventType,
        #            _aggregateType,
        #            _aggregateId,
        #            _idempotencyKey,
        #            _correlationId);
        #
        #        if (!sentOk)
        #            warning(strFmt('IND_INTERNAL_API mail: SendMailEx devolvio false aggregateId=%1 eventType=%2 correlationId=%3.',
        #                           _aggregateId, _eventType, _correlationId));
        #        else
        #            info(strFmt('IND_INTERNAL_API mail: SendMailEx aceptado aggregateId=%1 eventType=%2 correlationId=%3.',
        #                        _aggregateId, _eventType, _correlationId));
        #    }
        #    catch (Exception::DDEerror)
        #    {
        #        warning(strFmt('IND_INTERNAL_API mail: error DDE/COM enviando email aggregateId=%1 eventType=%2 correlationId=%3.',
        #                       _aggregateId, _eventType, _correlationId));
        #        sentOk = false;
        #    }
        #    catch (Exception::Error)
        #    {
        #        warning(strFmt('IND_INTERNAL_API mail: error enviando email aggregateId=%1 eventType=%2 correlationId=%3.',
        #                       _aggregateId, _eventType, _correlationId));
        #        sentOk = false;
        #    }
        #    catch (Exception::Internal)
        #    {
        #        warning(strFmt('IND_INTERNAL_API mail: error interno enviando email aggregateId=%1 eventType=%2 correlationId=%3.',
        #                       _aggregateId, _eventType, _correlationId));
        #        sentOk = false;
        #    }
        #
        #    clientAPI = null;
        #
        #    return sentOk;
        #}
        #
      ENDSOURCE
    ENDMETHODS
  ENDCLASS

***Element: END
