Exportfile for AOT version 1.0 or later
Formatversion: 1

***Element: JOB

; Navision Axapta Job: Job_INDInternalApi_SendMail
; --------------------------------------------------------------------------------
  JOBVERSION 1

  SOURCE #Job_INDInternalApi_SendMail
    #static void Job_INDInternalApi_SendMail(Args _args)
    #{
    #    COM     clientt;
    #    INDDefaultParameters defaultParameters = INDDefaultParameters::find();
    #
    #    str     baseUrl         = defaultParameters.InternalAPIUrlService;
    #    str     apiUser         = defaultParameters.APIUserId;
    #    str     apiPassword     = defaultParameters.INDAPIIntServiceUser().Password;
    #
    #    str     fromEmail       = "marco.meza@insertec.biz";
    #    str     fromName        = "";
    #    str     toEmails        = "marco.meza@insertec.biz";
    #    str     ccEmails        = "";
    #    str     bccEmails       = "";
    #    str     replyToEmails   = "";
    #    str     company         = "LAN";
    #
    #    // Staged absolute paths separated by semicolon. Empty means no attachments.
    #    str     attachmentFilePaths = "";
    #
    #    str     correlationId;
    #    str     aggregateId;
    #    str     idempotencyKey;
    #    str     healthJson;
    #    boolean online;
    #    boolean sent;
    #    ;
    #
    #    correlationId  = strFmt("ax-mail-sendmailex-min-%1-%2", today(), timeNow());
    #    aggregateId    = strFmt("%1-%2", today(), timeNow());
    #    idempotencyKey = strFmt("AX-MailSendMailExMinimal:%1", aggregateId);
    #
    #    try
    #    {
    #        clientt = new COM("IND.InternalApiClient");
    #
    #        healthJson = clientt.GetApiHealthStatusJson(baseUrl, correlationId);
    #        info(strFmt("Health JSON: %1", healthJson));
    #
    #        online = clientt.IsApiOnline(baseUrl, correlationId);
    #        if (!online)
    #        {
    #            error("IND_INTERNAL_API is not online from AOS.");
    #            clientt = null;
    #            return;
    #        }
    #
    #        sent = clientt.SendMailEx(
    #            baseUrl,
    #            apiUser,
    #            apiPassword,
    #            fromEmail,
    #            fromName,
    #            toEmails,
    #            ccEmails,
    #            bccEmails,
    #            replyToEmails,
    #            "Prueba mail SendMailEx IND_INTERNAL_API desde Axapta",
    #            "<p>Prueba de envio desde Axapta usando SendMailEx.</p>",
    #            "Prueba de envio desde Axapta usando SendMailEx.",
    #            attachmentFilePaths,
    #            false,
    #            "normal",
    #            company,
    #            "AXAPTA",
    #            "ManualSmokeTest",
    #            "MailSendMailExMinimalSmokeTest",
    #            "ManualTest",
    #            aggregateId,
    #            idempotencyKey,
    #            correlationId);
    #
    #        if (sent)
    #            info("SendMailEx OK. Microsoft Graph accepted the request.");
    #        else
    #            error("SendMailEx returned false. Review internal.mail.send, Graph settings and API logs.");
    #    }
    #    catch (Exception::Error)
    #    {
    #        error("Error executing COM IND.InternalApiClient SendMailEx smoke test.");
    #    }
    #
    #    clientt = null;
    #}
  ENDSOURCE

***Element: END