Exportfile for AOT version 1.0 or later
Formatversion: 1

***Element: JOB

; Navision Axapta Job: Job_INDInternalApi_SendMail_Extendido
; --------------------------------------------------------------------------------
  JOBVERSION 1

  SOURCE #Job_INDInternalApi_SendMail_Extendido
    #//MMS - Integra la prueba completa de correo con el wrapper central V2 - 2026.08.12
    #static void Job_INDInternalApi_SendMail_Extendido(Args _args)
    #{
    #    INDDefaultParameters defaultParameters = INDDefaultParameters::find();
    #
    #    str     baseUrl         = defaultParameters.InternalAPIUrlService;
    #
    #    str     fromEmail       = "i.jauregi@insertec.biz";
    #    str     fromName        = "Prueba Axapta";
    #    str     toEmails        = "marco.meza@insertec.biz";
    #    str     ccEmails        = "";
    #    str     bccEmails       = "";
    #    str     replyToEmails   = "";
    #    str     company         = "";
    #
    #    // Staged absolute paths separated by semicolon. Empty means no attachments.
    #    str     attachmentFilePaths = "";
    #
    #    str     htmlBody;
    #    str     textBody;
    #    str     correlationId;
    #    str     aggregateId;
    #    str     idempotencyKey;
    #    boolean online;
    #    boolean sent;
    #    ;
    #
    #    correlationId  = strFmt("ax-mail-extended-%1-%2", today(), timeNow());
    #    aggregateId    = strFmt("%1-%2", today(), timeNow());
    #    idempotencyKey = strFmt("AX-MailExtendedTest:%1", aggregateId);
    #
    #    //MMS - Detiene la prueba antes de la red si falta un buzon aprobado - 2026.08.12
    #    if (fromEmail == "" || toEmails == "")
    #    {
    #        error("Configure fromEmail y toEmails con buzones de prueba aprobados antes de ejecutar el job.");
    #        return;
    #    }
    #
    #    htmlBody = "<p>Prueba extendida de envío desde Axapta usando IND_INTERNAL_API.</p>" +
    #               "<p>Este envío prueba CC, BCC, Reply-To, saveToSentItems e importance.</p>";
    #    textBody = "Prueba extendida de envío desde Axapta usando IND_INTERNAL_API.";
    #
    #    try
    #    {
    #        online = INDInternalApiClientServer::isApiOnline(baseUrl, correlationId);
    #        if (!online)
    #        {
    #            error("La API interna no está online o no responde desde Axapta.");
    #            return;
    #        }
    #
    #        sent = INDInternalApiClientServer::sendInternalApiMailEx(
    #            company,
    #            fromEmail,
    #            fromName,
    #            toEmails,
    #            ccEmails,
    #            bccEmails,
    #            replyToEmails,
    #            "Prueba extendida mail IND_INTERNAL_API desde Axapta",
    #            htmlBody,
    #            textBody,
    #            attachmentFilePaths,
    #            false,
    #            "high",
    #            "ManualSmokeTest",
    #            "MailExtendedSmokeTest",
    #            "ManualTest",
    #            aggregateId,
    #            idempotencyKey,
    #            correlationId);
    #
    #        if (sent)
    #            info("SendMailEx OK. Microsoft Graph aceptó la petición.");
    #        else
    #            error("SendMailEx devolvió false. Revisar scope, permisos Graph, configuración y logs de la API.");
    #    }
    #    catch (Exception::Error)
    #    {
    #        error("Error ejecutando la prueba extendida mediante el cliente COM V2 central.");
    #    }
    #}
  ENDSOURCE

***Element: END
