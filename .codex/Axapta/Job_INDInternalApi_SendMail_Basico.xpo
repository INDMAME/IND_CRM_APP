Exportfile for AOT version 1.0 or later
Formatversion: 1

***Element: JOB

; Navision Axapta Job: Job_INDInternalApi_SendMail_Basico descargados en Miércoles 27/05/2026
; --------------------------------------------------------------------------------
  JOBVERSION 1

  SOURCE #Job_INDInternalApi_SendMail_Basico
    #static void Job_INDInternalApi_SendMail_Basico(Args _args)
    #{
    #    COM     clientt;
    #    INDDefaultParameters defaultParameters = INDDefaultParameters::find();
    #
    #    // URL base de la API interna DEV.
    #    str     baseUrl         = defaultParameters.InternalAPIUrlService;
    #
    #    // Usuario y password del cliente interno de la API.
    #    str     apiUser         =  defaultParameters.APIUserId;
    #    str     apiPassword     = defaultParameters.INDAPIIntServiceUser().Password;
    #
    #    // Buzón remitente. Debe ser un usuario/mailbox válido para Microsoft Graph.
    #    str     fromEmail       = "i.jauregi@insertec.biz";
    #
    #    // Destinatarios principales. Para varios emails, separar con punto y coma (;).
    #    str     toEmails        = "marco.meza@insertec.biz";
    #
    #    // Company opcional. Si se informa, debe estar permitida en el JWT del usuario API.
    #    str     company         = "";
    #
    #    // Identificadores para poder rastrear la prueba en logs.
    #    str     correlationId;
    #    str     aggregateId;
    #    str     idempotencyKey;
    #
    #    str     healthJson;
    #    boolean online;
    #    boolean sent;
    #    ;
    #
    #    correlationId  = strFmt("ax-mail-basic-%1-%2", today(), timeNow());
    #    aggregateId    = strFmt("%1-%2", today(), timeNow());
    #    idempotencyKey = strFmt("AX-MailBasicTest:%1", aggregateId);
    #
    #    try
    #    {
    #        // Crea la instancia COM registrada en el AOS/cliente.
    #        clientt = new COM("IND.InternalApiClient");
    #
    #        // Comprueba que la API responde antes de intentar enviar email.
    #        healthJson = clientt.GetApiHealthStatusJson(baseUrl, correlationId);
    #        info(strFmt("Health JSON: %1", healthJson));
    #
    #        online = clientt.IsApiOnline(baseUrl, correlationId);
    #        if (!online)
    #        {
    #            error("La API interna no está online o no responde desde Axapta.");
    #            clientt = null;
    #            return;
    #        }
    #
    #        // Envía un email básico: remitente, destinatarios, asunto y cuerpo.
    #        sent = clientt.SendMail(
    #            baseUrl,
    #            apiUser,
    #            apiPassword,
    #            fromEmail,
    #            toEmails,
    #            "Prueba básica mail IND_INTERNAL_API desde Axapta",
    #            "<p>Prueba básica de envío desde Axapta usando IND_INTERNAL_API.</p>",
    #            "Prueba básica de envío desde Axapta usando IND_INTERNAL_API.",
    #            company,
    #            "AXAPTA",
    #            "ManualSmokeTest",
    #            "MailBasicSmokeTest",
    #            "ManualTest",
    #            aggregateId,
    #            idempotencyKey,
    #            correlationId);
    #
    #        if (sent)
    #            info("SendMail OK. Microsoft Graph aceptó la petición.");
    #        else
    #            error("SendMail devolvió false. Revisar scope, permisos Graph, configuración y logs de la API.");
    #    }
    #    catch (Exception::Error)
    #    {
    #        error("Error ejecutando prueba básica COM IND.InternalApiClient.");
    #    }
    #
    #    clientt = null;
    #}
  ENDSOURCE

***Element: END
