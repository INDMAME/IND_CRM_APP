Exportfile for AOT version 1.0 or later
Formatversion: 1

***Element: JOB

; Navision Axapta Job: INDJobDeleteLegacyVisitasAccess
; --------------------------------------------------------------------------------
  JOBVERSION 1

  SOURCE #INDJobDeleteLegacyVisitasAccess
    #static void INDJobDeleteLegacyVisitasAccess(Args _args)
    #{
    #    boolean                 execute = false;
    #    str                     appCode = "CRM";
    #    str                     legacyCreateModule = "VISITAS_CREACION";
    #    str                     legacyHistoryModule = "VISITAS_HISTORIAL";
    #
    #    INDWebModuleAccessLevel legacyAccess;
    #
    #    str                     executeMode;
    #    int                     rowsFound = 0;
    #    int                     rowsDeleted = 0;
    #    ;
    #
    #    // Set execute to true only after checking the preview counter.
    #    if (execute)
    #        executeMode = "true";
    #    else
    #        executeMode = "false";
    #
    #    info(strFmt("Legacy visitas access cleanup started. Execute mode: %1", executeMode));
    #
    #    if (execute)
    #    {
    #        ttsbegin;
    #
    #        while select forUpdate legacyAccess
    #            where legacyAccess.AppCode == appCode
    #               && (legacyAccess.ModuleCode == legacyCreateModule
    #                || legacyAccess.ModuleCode == legacyHistoryModule)
    #        {
    #            rowsFound++;
    #            legacyAccess.delete();
    #            rowsDeleted++;
    #        }
    #
    #        ttscommit;
    #    }
    #    else
    #    {
    #        while select legacyAccess
    #            where legacyAccess.AppCode == appCode
    #               && (legacyAccess.ModuleCode == legacyCreateModule
    #                || legacyAccess.ModuleCode == legacyHistoryModule)
    #        {
    #            rowsFound++;
    #        }
    #    }
    #
    #    info(strFmt("Legacy VISITAS_CREACION/VISITAS_HISTORIAL records found: %1", rowsFound));
    #    info(strFmt("Legacy records deleted: %1", rowsDeleted));
    #
    #    if (!execute)
    #    {
    #        info("Preview only. Change execute to true to delete legacy visitas access records.");
    #    }
    #}
  ENDSOURCE

***Element: END
